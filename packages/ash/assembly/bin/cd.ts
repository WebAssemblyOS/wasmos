import { os } from "../../../../packages/os";

export function main(args: string[]): void {
  let dir = args[1]; //or fs.getDir(file);
  if(!dir) {
    //os.homedir
  }

/*
Notes: path_open opens directory
let plat = platform();
let fee = fs.getDir;
let env = Environ.get("HOME");
let dirname = args[1];
env = Environ.get("$PATH");
let home = env.get("HOME")!;
  */
}
