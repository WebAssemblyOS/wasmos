
import { Console, fs, Process, CommandLine } from '../wasa/mock/index';
/** This file is included with tests so that the default globals are set up properly. */
export { Console, fs, Process, CommandLine }
type fd = usize;

